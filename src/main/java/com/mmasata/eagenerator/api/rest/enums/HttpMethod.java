package com.mmasata.eagenerator.api.rest.enums;

/**
 * Enum defining the supported types of HTTP methods.
 */
public enum HttpMethod {
    GET,
    POST,
    PUT,
    DELETE,
    PATCH,
    OPTIONS;
}